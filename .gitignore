# Secrets and credentials
*.key
*.pem
*.crt
*.env
.env*
secrets.yaml
*-secret.yaml
kubeconfig
credentials.json

# Helm
charts/*.tgz
*.lock

# Kustomize
kustomize

# Terraform
.terraform/
*.tfstate
*.tfstate.backup
*.tfvars

# Editor/IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS files
.DS_Store
Thumbs.db

# Temporary files
tmp/
temp/
*.tmp
*.bak

# Local tooling / binaries (don't commit)
aws/
awscliv2.zip
kubectl
kubectl.sha256

# Local-machine training scripts (use the in-repo run.sh instead)
trainSmolVLA.sh

# Local checkpoint downloads (e.g. via `kubectl cp` from training pods)
g1_finetune/