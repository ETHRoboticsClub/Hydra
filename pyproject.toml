[project]
name = "hydra"
version = "1.0.0"
description = "Kubernetes resource creation TUI for ML training jobs"
requires-python = ">=3.10"
dependencies = [
    "textual>=0.50.0",
]

[project.scripts]
create = "create:main"
